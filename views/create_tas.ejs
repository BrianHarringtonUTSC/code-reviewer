<!DOCTYPE html>
<html >
  <head>
    <meta charset="UTF-8">
    <title>TA's</title>
    <link rel="stylesheet" href="//netdna.bootstrapcdn.com/font-awesome/4.2.0/css/font-awesome.min.css">
    <link rel="stylesheet" type="text/css" href="stylesheets/nav.css">
  </head>

  <body>
    <% include templates/instructor_header.ejs %>

    <form action="create_tas/load" method="post" enctype="multipart/form-data">
      <input type="file" value ="Upload tas file" name="rosters" required><br>
      <input type="submit" value ="Load new tas">
    </form>

    <p> All TA's </p>
    <p> number of TA's is <%= num_tas %> </p>
    <section class="ac-container">
      <% tas.forEach( function(item) { %>
      <p><%= item.utorid + ' ' + item.email + ' ' + item.student_number + '---------' + item.weight %></p>
      <% }); %>
    </section>
  </body>
</html>
